let elem = document.querySelector("div.msg");
// Получаем название класса
console.log(elem.className);

// Если хотим увидить оформление атрибута style 
let elem2 = elem.nextSibling.nextSibling; 
console.log(elem2.style.top);